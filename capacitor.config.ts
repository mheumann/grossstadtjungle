import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.mheumann.app',
  appName: 'Grossstadtjungle',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;


